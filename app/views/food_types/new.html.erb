<div class="page-shell">
  <section class="page-intro page-intro--split">
    <div>
      <p class="eyebrow">Admin</p>
      <h1>Add a dish type</h1>
      <p class="lede">Use dish labels that feel obvious wherever people browse the collection.</p>
    </div>
    <div class="info-card">
      <p class="eyebrow">Shorter is better</p>
      <p>Clear, compact names make recipe cards and filters easier to understand.</p>
    </div>
  </section>

  <section class="section-card section-card--form">
    <%= render partial: 'form', locals: { submit_text: 'Add dish type' } %>
  </section>
</div>
